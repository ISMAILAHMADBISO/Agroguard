{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c green,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev --workspace @workspace/api-server",
    "dev:web": "npm run dev --workspace @workspace/agroguard",
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "vercel-build": "npm run build --workspace @workspace/agroguard",
    "start": "cross-env NODE_ENV=production SERVE_CLIENT=true npm run start --workspace @workspace/api-server",
    "setup": "npm run push --workspace @workspace/db && npm run setup --workspace @workspace/scripts",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck --workspace @workspace/agroguard --workspace @workspace/api-server --workspace @workspace/mockup-sandbox --workspace @workspace/scripts --if-present"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^10.1.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "overrides": {
    "@esbuild-kit/esm-loader": "npm:tsx@^4.21.0",
    "esbuild": "0.27.3"
  }
}
